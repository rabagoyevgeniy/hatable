{
  "name": "hatable",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Stranded Mars — a Mars survival-crafting game inspired by The Martian and Stranded Deep",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "meshy": "node scripts/meshy-generate.mjs",
    "meshy:motion": "node scripts/meshy-motion.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
